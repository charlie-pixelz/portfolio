# Charlie Pixelz — variables de entorno
# v1 es un sitio 100% estático en GitHub Pages: NO hay secretos ni tokens en el frontend.
# Todo lo que llega al navegador es público (regla de fable-seguridad).
#
# Si más adelante se suma analytics sin cookies (GoatCounter), su dominio va aquí como
# variable PÚBLICA de build (no es un secreto):
# VITE_ANALYTICS_DOMAIN=
